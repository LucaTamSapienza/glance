# Build artifacts
/glance
/glance-render
/build/
/build-t-*
/.san-probe*
/dist/
*.o
*.test
*.out
*.dSYM/
.DS_Store
build.log
testdata/todo.md
/.buildflags

# local embedding spike (llama.cpp build + models, not committed)
/spike/

# per-vault embedding cache (content-addressed, regenerable)
.glance/
